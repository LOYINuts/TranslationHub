# 翻译工作流

## 文件角色

| 类型 | 可编辑源 | 产物/目标 |
|---|---|---|
| `mods.toml` line/json | 模组目录内 `translations.json` | 配置的中文 TXT/INI/JSON |
| script | 脚本使用的翻译源 | 脚本生成文件 |
| xTranslator XML | `pending.json` | XML 中 `TRADUIT` |
| Descriptionmods | `Descriptionmods/zh/...` | 同一文件 |
| Qt TS | `Eslifer/eslifier_translation.ts` | 同一文件 |

有 `translations.json` 时禁止直接改中文产物。英文源默认只读；仅同步上游版本时修改。

## 日常流程

```bash
# 1. 检查上游是否新增行文本键
python sync_translation.py <mod> --check

# 2. 编辑 translations.json 后构建
python build_all.py <mod>

# 3. 验证键、占位符、转义和产物是否最新
python verify_translation.py <mod>
```

全量检查：

```bash
python sync_translation.py --all --check
python build_all.py
python verify_translation.py
```

命令参数接受 `mods.toml` 中的完整 `dir` 或目录 basename。未知名称和任何验证错误均返回非零退出码。

## 编码与格式

```bash
python locale_utils.py detect <file>
python locale_utils.py dump <file>
```

编码、分隔符和输出文件名以 `mods.toml` 为准。必须保留键、节、顺序、分隔符、注释、数字、标签、占位符和转义，包括 `%s`、`%llu`、`%1`、`{0}`、`$VAR$`、`<font>`、`[br]`、字面量 `\\n`。

## 添加配置模组

在 `mods.toml` 添加 `[[line]]`、`[[json]]` 或 `[[script]]`。script 任务必须声明生成脚本和验证脚本：

```toml
[[script]]
dir = "path/to/mod"
script = "generate_zh.py"
verify = "check_keys.py"
```

不在 Python 脚本中维护模组列表。项目要求 Python 3.11+，只使用标准库。

## 特殊类型

- Dialogue XML：按 `.agents/skills/dialogue-xml/SKILL.md` 执行 `stats → pending → apply → stats → --self-check`。
- Descriptionmods：直接编辑 `Descriptionmods/zh/`，运行 `python verify_translation.py Descriptionmods`。
- Qt TS：编辑 `Eslifer/eslifier_translation.ts`，运行 `python verify_translation.py Eslifer`。
- 全量 `verify_translation.py` 自动覆盖以上两类和全部配置模组；Dialogue XML 保持独立验证。
