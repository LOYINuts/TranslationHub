# 翻译工作流

## 统一流程（推荐）

所有模组使用 `translations.json` + `build_all.py` 工作流：

```bash
# 1. 编辑翻译内容
vim <mod_dir>/translations.json

# 2. 构建输出文件
python build_all.py <mod_name>

# 3. 验证翻译
python verify_translation.py <mod_name>

# 4. 提交
git add -A
git commit -m "更新 <mod_name> 翻译"
git push
```

---

## 目录结构

```
项目根目录/
├── mcm_flick_translation/          # MCM/FLICK 菜单翻译
│   ├── FUCK/
│   │   ├── translations.json       # 翻译内容（编辑这个）
│   │   ├── FUCK_ENGLISH.txt        # 英文源（不改）
│   │   └── FUCK_CHINESE.txt        # 中文输出（自动生成）
│   ├── fittingroom/
│   └── CEE/
├── mod_interface_translation/      # 界面翻译
│   ├── musicconductor/
│   ├── custommarkers/
│   └── PartySheet/
├── skse_menu_translation/          # SKSE 菜单翻译
│   ├── SKSE Menu Framework/
│   └── Viny Mods/
├── dialogue_mod_translation/       # 对话翻译（XML）
└── Descriptionmods/                # 描述翻译
    └── zh/                         # 直接编辑这里
```

---

## 编码规范

| 目录 | 编码 | 分隔符 |
|------|------|--------|
| mcm_flick_translation/ | UTF-16 LE BOM | `\t` |
| mod_interface_translation/ | UTF-8 | `=` 或 ` = ` |
| skse_menu_translation/ | UTF-16 LE BOM | `\t` 或 `=` |

**检测编码：**
```bash
python locale_utils.py detect <file>
```

---

## 批量操作

### 构建所有模组
```bash
python build_all.py
```

### 验证所有模组
```bash
python verify_translation.py
```

### 统计翻译条目
```bash
python build_all.py --stats
```

### 构建指定模组
```bash
python build_all.py FUCK fittingroom CEE
```

---

## 翻译规则

### 必须保留
- ✅ 键名（如 `$FUCK_A1`）
- ✅ 条目顺序
- ✅ 占位符：`%s` `%d` `{0}` `$VAR$`
- ✅ 标签：`<color>` `<font>` `[br]`
- ✅ 转义：`\n` `\t`（不是真实换行）

### 翻译原则
- 自然中文，不逐词翻译
- 对话像人说话
- UI 简洁清楚
- 术语保持一致

### 示例
```json
{
  "$FUCK_A1": "Freeze Time",           // ❌ 英文
  "$FUCK_A1": "冻结时间",              // ✅ 中文
  
  "$FUCK_A2": "You have %d items",     // 占位符
  "$FUCK_A2": "你有 %d 个物品",        // ✅ 保留 %d
  
  "$FUCK_A3": "Press\\nto continue",   // 转义
  "$FUCK_A3": "按下\\n继续"            // ✅ 保留 \n
}
```

---

## 添加新模组

### 1. 创建 translations.json
```bash
cd <new_mod_dir>
# 从英文源提取键
python -c "
from locale_utils import read_lines
import json

lines = read_lines('MOD_ENGLISH.txt')
trans = {}
for line in lines:
    if '\t' in line:
        key, val = line.split('\t', 1)
        trans[key] = ''  # 待翻译

with open('translations.json', 'w', encoding='utf-8') as f:
    json.dump(trans, f, indent=2, ensure_ascii=False)
"
```

### 2. 添加到 build_all.py
```python
MOD_CONFIGS: list[ModConfig] = [
    # ...
    ModConfig(
        "your_dir/new_mod",
        "MOD_ENGLISH.txt",
        "MOD_CHINESE.txt",
        sep="\t",              # 或 "=" 或 " = "
        encoding="utf16-le-bom"  # 或 "utf-8"
    ),
]
```

### 3. 构建并验证
```bash
python build_all.py new_mod
python verify_translation.py new_mod
```

---

## 常见问题

### Q: 修改后没生效？
```bash
# 确认编辑的是 translations.json
# 然后重新构建
python build_all.py <mod_name>
```

### Q: 编码错误？
```bash
# 检测编码
python locale_utils.py detect <file>

# 查看内容
python locale_utils.py dump <file>
```

### Q: 验证失败？
```bash
# 查看详细错误
python verify_translation.py <mod_name>

# 常见错误：
# - 占位符遗漏：检查 %s %d
# - 键不匹配：检查 translations.json 键名
# - 转义错误：\n 不要写成真实换行
```

---

## 当前状态

- **总计：** 6634 条翻译
- **模组数：** 45+ 个
- **工作流：** 100% 统一（所有模组都使用 translations.json）
- **编码处理：** 自动检测 UTF-16/UTF-8
- **验证：** 自动检查键完整性、占位符、转义
